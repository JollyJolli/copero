{
  "name": "copero-career-editor",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "pretest": "node scripts/extract-clubs.mjs",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "test": "node --test"
  },
  "devDependencies": { "esbuild": "^0.25.0" }
}
