import test from 'node:test'; import assert from 'node:assert/strict'; import { ErrorHandler, installFailureApi } from '../src/core/error-handler.js';
test('captures command failures and keeps recovery data',()=>{const runtime={};const h=new ErrorHandler({prefix:'careerEditor.',version:'B1',debug:false},runtime);const old={group:console.group,error:console.error,warn:console.warn,info:console.info,groupEnd:console.groupEnd};Object.assign(console,{group(){},error(){},warn(){},info(){},groupEnd(){}});try{assert.equal(h.guard('boom',()=>{throw new Error('read only property name')}),undefined);assert.equal(runtime.lastError.command,'boom');assert.match(runtime.lastError.likelyCause,/propiedad protegida/);}finally{Object.assign(console,old);}});
test('installs diagnostic fallback API after fatal boot error',()=>{const target={};const report={message:'boom'};const api=installFailureApi(target,'careerEditor',{version:'3',prefix:'careerEditor.'},report);assert.equal(target.careerEditor,api);assert.equal(api.installationFailed,true);assert.equal(api.diagnose(),report);});
