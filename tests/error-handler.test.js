import test from 'node:test'; import assert from 'node:assert/strict'; import { ErrorHandler, installFailureApi } from '../src/core/error-handler.js';
const silenceConsole=action=>{const names=['group','groupCollapsed','log','error','warn','info','debug','groupEnd'],old=Object.fromEntries(names.map(name=>[name,console[name]]));Object.assign(console,Object.fromEntries(names.map(name=>[name,()=>{}])));try{return action();}finally{Object.assign(console,old);}};
test('captures command failures and keeps recovery data',()=>{const runtime={};const h=new ErrorHandler({prefix:'careerEditor.',version:'B2',debug:false},runtime);silenceConsole(()=>{assert.equal(h.guard('boom',()=>{throw new Error('read only property name')}),undefined);});assert.equal(runtime.lastError.command,'boom');assert.match(runtime.lastError.likelyCause,/propiedad protegida/);});
test('installs diagnostic fallback API after fatal boot error',()=>{const target={};const report={message:'boom'};const api=installFailureApi(target,'careerEditor',{version:'B2',prefix:'careerEditor.'},report);assert.equal(target.careerEditor,api);assert.equal(api.installationFailed,true);assert.equal(silenceConsole(()=>api.diagnose()),report);});
