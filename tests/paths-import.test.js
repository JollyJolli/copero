import test from'node:test';import assert from'node:assert/strict';import{Validator}from'../src/core/validator.js';import{setAtPath,getAtPath,deleteAtPath}from'../src/core/paths.js';import{isGameState}from'../src/core/react-locator.js';import{mockState}from'./helpers.js';
test('path operations',()=>{const v=new Validator({});const x={};setAtPath(x,'a[0].b',3,v);assert.equal(getAtPath(x,'a.0.b',v),3);assert.ok(deleteAtPath(x,'a.0.b',v));});test('detects valid game state and wrapped imports',()=>{const s=mockState();assert.ok(isGameState(s));assert.ok(isGameState({gameState:s}.gameState));assert.ok(!isGameState({}));});
