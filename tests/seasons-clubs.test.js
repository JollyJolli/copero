import test from'node:test';import assert from'node:assert/strict';import{resolveSeasonIndexes}from'../src/modules/helpers.js';import{ClubCatalog,replaceOfferState}from'../src/modules/clubs.js';import{mockState,mockStateManager}from'./helpers.js';
test('season selectors',()=>{const s=mockState();s.seasons.push({...s.seasons[0],id:'s2'});assert.deepEqual(resolveSeasonIndexes(s,'last'),[1]);assert.deepEqual(resolveSeasonIndexes(s,1),[0]);assert.deepEqual(resolveSeasonIndexes(s,'s2'),[1]);});test('club search and offer replacement',()=>{const s=mockState();s.seasons.push({...s.seasons[0],teamId:'barcelona'});const c=new ClubCatalog(mockStateManager(s));c.refresh();assert.equal(c.search('BARC')[0].id,'barcelona');replaceOfferState(s,1,c.getById('barcelona'));assert.equal(s.currentEvent.options[0].teamId,'barcelona');});test('rejects without compatible template',()=>{const s=mockState();s.currentEvent.options=[{id:'x'}];assert.throws(()=>replaceOfferState(s,1,{id:'barcelona'}));});
